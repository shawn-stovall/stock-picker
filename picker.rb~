=begin
  Method that picks the best buy and
  sell days for a stock, to maximize
  profit.
=end
def stock_picker(days)
  buy_day = 0
  sell_day = 1
  highest_sell = days[1] - days[0]

  days.each_with_index do |b_value, b_day|
    days.drop(b_day + 1).each_with_index do |s_value, s_day|
      hs = s_value - b_value
      if hs > highest_sell
        buy_day = b_day
        sell_day = s_day
        highest_sell = hs
      end
    end
  end

  return [buy_day, sell_day]
end
        
